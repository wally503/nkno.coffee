// src/pages/brewHistory/BrewHistoryLayout.jsx
//
// Same caveat as BrewNewLayout — shape inferred, not copied from a real file.

import { Outlet } from "react-router-dom";

export default function BrewHistoryLayout() {
  return <Outlet />;
}